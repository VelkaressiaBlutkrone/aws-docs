# ③ 학습문서 사실성 감사 샤드 — 03-docs-soa-c (t3-1~t3-4·t4-1) — 2026-07

## 요약 (3~5줄)

- 대상 5개 문서(soa-t3-1·t3-2·t3-3·t3-4·t4-1)를 전문 정독. 전반 품질은 높고 핵심 시험 관례(변경 세트=미리보기, 드리프트=탐지만, StackSets=다계정, Deny 우선, SCP=상한선)는 정확함. 발견 총 13건(H 0 / M 5 / L 8), 사실의심 11건.
- M급 핵심: ① t3-1 스택 생성 실패 기본 동작이 "스택 제거"로 읽힘(실제 기본은 `ROLLBACK_COMPLETE` 잔존→수동 삭제 필요), ② t3-1 RDS 기본 DeletionPolicy 예외(기본 Snapshot) 미반영으로 함정·Q4가 과단정, ③ t3-2 암호화 AMI 공유에서 AWS 관리형 기본 키는 공유 자체 불가(CMK 필요) 누락, ④ t4-1 인라인 정책 부착 대상에서 그룹 누락, ⑤ t4-1 "특정 S3/SCP 우회"가 루트의 SCP 우회로 오독됨(같은 문서 §7·Q1의 "SCP는 루트에도 적용"과 모순).
- 문서 내 모순 2건(t3-1 §7 표의 "기본값 대부분" 헤지 vs 함정2·Q4의 단정 / t4-1 §7 첫 단락 vs 뒷단락·Q1). 샤드 문서 간(t3-2↔t3-3) 모순은 없고, Patch Group 태그 키 단정은 두 문서 공통(207).
- 확신도 낮·중 항목(202·203·207·208·210)은 최근 사양 변경/문서 개정 가능성이 있어 2단 검증 대상. 공식 가이드 v1.1 개정 건은 별도 발견 완료로 본 리포트에서 제외.

## 발견 항목

| ID | 위치 | 발견 내용 | 심각도(H/M/L) | 확신도(높/중/낮) | 권장 조치 | Phase(A/B) | 사실의심(Y/N) |
|---|---|---|---|---|---|---|---|
| DOC-SOA-201 | assets/content/soa/soa-t3-1.md#3-스택-생성업데이트와-변경-세트change-set (롤백 절) + 자가 점검 Q5 | "생성 실패 시 … 부분 생성된 리소스를 정리하고 스택을 제거(또는 `ROLLBACK_COMPLETE`)" — 기본 동작(OnFailure=ROLLBACK)은 스택이 **ROLLBACK_COMPLETE 상태로 잔존**하며 같은 이름 재생성 전 수동 삭제가 필요. "제거"는 OnFailure=DELETE를 지정했을 때만. Q5 답의 "스택이 존재하거나 없거나 두 상태만"도 동일 오개념 강화(ROLLBACK_COMPLETE는 리소스 없이 존재하는 제3 상태). 근거: CLI `create-stack --on-failure` 기본값 ROLLBACK, 실패 스택은 ROLLBACK_COMPLETE에서 업데이트 불가·삭제만 가능 | M | 높 | 기본=ROLLBACK_COMPLETE 잔존(재생성 전 삭제 필요), 제거는 DELETE 옵션임을 명시; Q5 답 표현 보정 | A | Y |
| DOC-SOA-202 | assets/content/soa/soa-t3-1.md#7-deletionpolicy--삭제-시-데이터-보호 (원리 박스) | "Snapshot을 지원하지 않는 리소스에 이 값을 지정하면 CloudFormation은 오류를 반환합니다" — 미지원 리소스 지정 시 동작(즉시 검증 오류 vs 무시/기본 정책 적용)이 공식 문서에서 명시 확인 안 됨. 근거: DeletionPolicy 공식 문서는 Snapshot 지원 리소스 목록만 제시하고 미지원 시 오류 반환을 단정하는 문장은 기억상 부재 — 2단 검증 필요 | L | 낮 | 실측/공식 문서 재확인 후 단정 유지 또는 "지원 목록 외에는 사용할 수 없다"로 완화 | B | Y |
| DOC-SOA-203 | assets/content/soa/soa-t3-1.md#7-deletionpolicy--삭제-시-데이터-보호 (표·함정2) + 자가 점검 Q4 | 표는 "Delete (기본값 대부분)"로 헤지했으나 함정2("기본 동작은 함께 삭제입니다. RDS·EBS 등은 …명시해야 데이터가 보존")와 Q4 답("기본 동작은 스택과 함께 삭제이므로")은 무조건 단정 — AWS::RDS::DBCluster 및 DBClusterIdentifier 없는 DBInstance는 **기본 DeletionPolicy가 Snapshot**이라 RDS 단정은 부정확(문서 내 온도차·모순). 근거: CFN DeletionPolicy 공식 문서의 RDS 기본 Snapshot 예외 노트 | M | 중 | RDS 예외를 각주로 반영할지, 시험 관례상 "명시 안전" 프레임을 유지할지 사람 결정 + 2단 검증 | B | Y |
| DOC-SOA-204 | assets/content/soa/soa-t3-1.md#5-stacksets--다중-계정다중-리전-배포 + 시험 포인트·함정3 | "StackSets = 다중 계정 × 다중 리전 (전용)" 프레임 — StackSets는 **단일 계정 × 다중 리전**(셀프 관리형으로 자기 계정만 대상) 배포에도 표준 사용. "×" 표기가 둘 다 복수여야 한다는 오개념 소지(단일 계정·복수 리전 문항에서 주저 유발). 근거: StackSets 공식 정의 "across multiple accounts **and Regions**" + 단일 계정 다중 리전 스택 인스턴스 지원 | L | 높 | "다중 계정 **및/또는** 다중 리전"으로 자구 수정, 함정3은 "한 계정·한 리전" 한정 유지 | A | Y |
| DOC-SOA-205 | assets/content/soa/soa-t3-1.md#7-deletionpolicy--삭제-시-데이터-보호 (공식 정의 인용) | 인용문 "리소스를 보존하거나(백업) 백업할지 지정한다" — '백업' 중복으로 문장 성립 안 됨(원문: "preserve, and in some cases, backup"). 사실 오류라기보다 번역 자구 훼손 | L | 높 | "보존하거나, 경우에 따라 백업(스냅샷)할지 지정한다"로 정정 | A | N |
| DOC-SOA-206 | assets/content/soa/soa-t3-2.md#2-ami-공유와-리전-간-복사 + 함정3·자가 점검 Q1 | "암호화된 AMI를 공유하려면 사용한 KMS 키도 함께 공유해야" — **AWS 관리형 기본 키(aws/ebs)로 암호화된 스냅샷 기반 AMI는 공유 자체가 불가**(기본 키는 키 정책 공유 불가; 고객 관리형 키로 재암호화 필요)라는 전제 누락. 현행 서술은 "어떤 암호화 AMI든 키만 공유하면 된다"는 오개념 소지 — 시험에서 CMK 재암호화가 정답인 문항 존재. 근거: EC2 공식 문서 "You can't share an AMI backed by snapshots encrypted with the default AWS managed key" | M | 높 | "공유하려면 고객 관리형 키(CMK)로 암호화돼 있어야 하며, 기본 관리형 키 암호화본은 공유 불가(재암호화 필요)" 1줄 보강 | A | Y |
| DOC-SOA-207 | assets/content/soa/soa-t3-3.md#4-patch-manager--베이스라인패치-그룹유지-관리-기간 ("태그 키는 `Patch Group`(공백·대소문자 정확히)여야 인식") + soa-t3-2.md#5-패치-전략-개요 | 태그 키가 공백 포함 `Patch Group`**만** 인식된다는 단정 — 현행 Patch Manager는 공백 없는 **`PatchGroup`** 태그 키도 지원(공백 금지 태그 정책 계정 대응). "철자 주의" 교훈은 유효하나 배타 단정은 현행 문서와 불일치. 근거: SSM patch groups 공식 문서의 `Patch Group` 또는 `PatchGroup` 병기 | L | 중 | "`Patch Group`(또는 공백 없는 `PatchGroup`)"로 병기, 철자 주의 문구는 유지 | A | Y |
| DOC-SOA-208 | assets/content/soa/soa-t3-3.md#7-parameter-store-vs-secrets-manager (핵심 사실 3번째 불릿) | "자동 교체·**교차 계정 비밀 공유가 핵심이면 Secrets Manager**" — Parameter Store도 **고급 파라미터의 교차 계정 공유(AWS RAM)를 2024년부터 지원**해 차별점으로서의 교차 계정 단서가 약화됨(자동 교체 차별점은 여전히 유효). 시험 관례상 정답 유도는 아직 Secrets Manager라 완전 오류는 아니나 최신 사실과 어긋날 수 있음. 근거: 2024-02 Parameter Store cross-account sharing(advanced tier + RAM) 출시 | L | 중 | 2단 검증 후 교차 계정 문구 삭제 또는 "(고급 파라미터는 RAM 공유 가능)" 각주 — 시험 관례 유지 여부 사람 결정 | B | Y |
| DOC-SOA-209 | assets/content/soa/soa-t3-4.md#🔤-먼저-알아야-할-용어 (cron 식) | cron을 "분 시 일 월 요일" 5필드로 정의 — 이 문서에서 cron이 쓰이는 유일한 맥락인 **EventBridge 스케줄 규칙은 연도 포함 6필드**(`cron(분 시 일 월 요일 연도)`)이며 일·요일 동시 `*` 불가(`?` 필요). 5필드 그대로 쓰면 규칙 생성이 실패해 실습 혼선 소지. 근거: EventBridge 스케줄 표현식 공식 문서(6필드 필수) | L | 높 | "EventBridge에서는 연도 포함 6필드" 병기 | A | Y |
| DOC-SOA-210 | assets/content/soa/soa-t3-4.md#5-이벤트-기반-자동-교정-파이프라인 (예 3: 루트 계정 로그인) | "루트 계정 로그인(CloudTrail 이벤트) → EventBridge → SNS" — 콘솔 로그인(aws.signin)은 **글로벌 서비스 이벤트로 us-east-1 이벤트 버스에만 도착**하므로 규칙을 다른 리전(예: 서울)에 만들면 발화하지 않음. 리전 전제 누락으로 실습·운영 재현 실패 소지. 근거: AWS 지식 센터 루트 활동 모니터링 가이드(us-east-1에 규칙 생성 안내) | L | 중 | "(규칙은 us-east-1에 생성)" 괄호 보강 — 사양 변동 가능성 있어 2단 검증 병행 | B | Y |
| DOC-SOA-211 | assets/content/soa/soa-t4-1.md#1-정책-유형--부여하는-정책-vs-한도만-정하는-정책 (표) | 인라인 정책 행의 부착 대상이 "사용자·역할"로 **그룹 누락** — IAM 인라인 정책은 사용자·**그룹**·역할 모두에 임베드 가능. 관리형 행(사용자·그룹·역할)과의 대비 때문에 "그룹엔 인라인 불가"라는 오개념을 명시적으로 심는 표 구조. 근거: IAM 공식 "Inline policies … add directly to a single user, group, or role" + `PutGroupPolicy` API 존재 | M | 높 | 인라인 행 부착 대상을 "사용자·그룹·역할"로 정정 | A | Y |
| DOC-SOA-212 | assets/content/soa/soa-t4-1.md#7-루트-사용자-보호--scp-개요 (루트 전용 작업 나열) | "루트만 가능한 작업(계정 닫기, 결제 설정 일부, **특정 S3/SCP 우회** 등)" — "SCP 우회"가 루트 사용자는 SCP를 우회한다는 뜻으로 읽혀, 같은 절 뒷부분·시험 포인트·Q1의 "SCP는 **루트 사용자를 포함한** 모든 주체에 적용"과 **문서 내 정면 모순**. 의도는 'S3 MFA Delete 설정, 전체 Deny 버킷 정책 복구' 등 루트 전용 작업으로 추정되나 현재 표기는 시험 최다 출제 포인트(멤버 계정 루트에도 SCP 적용)를 훼손. 근거: Organizations 공식 "SCPs affect all users and roles in attached accounts, **including the root user**" | M | 높 | "특정 S3 작업(MFA Delete 설정·전체 Deny 버킷 정책 복구) 등"으로 재작성하고 "SCP 우회" 문구 삭제 | A | Y |
| DOC-SOA-213 | assets/content/soa/soa-t4-1.md#🧪-자가-점검 (Q5 답) | 관리 계정을 "조직을 생성·관리하는 **루트 계정**"으로 지칭 — '루트 사용자(root user)'와 용어 충돌. 같은 문단이 SCP·루트 사용자 관계를 다루는 맥락이라 혼동 위험이 상대적으로 큼 | L | 높 | "루트 계정" → "관리 계정(management account)"으로 통일 | A | N |

## 비고

- **발견 없음 아님** — 총 13건. H 0건 / M 5건(201·203·206·211·212) / L 8건. 사실의심 Y 11건(201~204·206~212 중 205·213 제외).
- 점검했으나 문제 없음(대표): 템플릿 필수 섹션(Resources 유일)·변경 세트 생성/실행 분리·Export 잠금·드리프트 탐지-만·서비스 관리형 자동 배포(t3-1) / AMI 리전 종속·deregister 후 스냅샷 잔존·Image Builder(t3-2) / 관리형 노드 3전제·`AmazonSSMManagedInstanceCore`·엔드포인트 3종(ssm·ssmmessages·ec2messages)·Run Command vs State Manager·Secrets Manager 자동 교체 차별점(t3-3) / `StatusCheckFailed_System` 복구=동일 인스턴스 이전(ID·IP·EBS 보존) vs ASG 교체·이벤트 패턴 vs 스케줄·Config+SSM Automation 교정(t3-4) / Deny 우선·SCP/경계=상한선·교집합 모델·BoolIfExists 원리·External ID·자격증명 보고서 vs 마지막 액세스(t4-1).
- 의도적 미보고(단순화로 판단, 오류 아님): SSM Default Host Management Configuration(인스턴스 프로파일 없이 관리 가능해진 신기능) 미언급, DeletionPolicy가 업데이트 중 리소스 제거 시에도 적용된다는 부연 생략, Session Manager 세션 데이터 로깅이 설정 필요하다는 부연, 리소스 기반 정책과 권한 경계의 평가 예외(동일 계정 직접 부여) 미기술, EC2 자동 복구의 인스턴스 유형·인스턴스 스토어 제약 생략.
