# Test Plan
Generated by /plan-eng-review on 2026-06-05
Branch: main
Repo: VelkaressiaBlutkrone/aws-docs

## Affected Pages/Routes
- `#/` (홈) — 자격증 카드: 콘텐츠 없는 자격증에 "준비 중 — 검증 문항 N개" 배지 표시 여부
- `#/cert/solutions-architect-associate` — 회귀: 학습문서 29개 렌더, 모의고사 랜덤+5회차, 325문항 전체 응시·채점 정상
- `#/cert/cloud-practitioner` — 빈 상태: 학습 탭 "준비 중"+공식 가이드 링크, 모의고사 시작 비활성, 죽은 링크 0
- `#/cert/{기타 10개}` — 가짜 생성 교재/문항이 어디에도 노출되지 않음

## Key Interactions to Verify
- 모의고사 회차 select 변경 → 폼 재렌더 → 제출 → 채점 결과(점수, 도메인별 통계) 표시
- 미응답 문항 포함 제출 → 오답 처리, throw 없음
- 오답 클릭 피드백: wrongExplanations 있으면 보기별 해설, 없으면 해설 목록 자체 생략(보일러플레이트 문구 금지)
- 회차 변경 후 재제출 (submit 리스너 중복 바인딩 여부)

## Edge Cases
- `<`, `>`, `&` 포함 문항 텍스트(prompt/option/caseText)가 깨지지 않고 표시 (escapeHtml 일관화)
- verified:false 문항이 prod 빌드에 미포함 (번들 grep + UI 미노출)
- 정답 letter가 options에 없는 raw 문항 → 빌드/테스트 단계에서 실패 (런타임 도달 금지)
- exam questionIds ⊆ questionBank IDs — 전 자격증 무결성 (getQuestion throw 원천 차단)

## Critical Paths
- [REGRESSION·CRITICAL] SAA: 일반화 이후에도 5회차 × 65문항 전체 ID 해석, 채점 결과 동일
- CLF(콘텐츠 추가 후): 학습문서 → 문항 풀이 → 오답 → 연결 학습문서 앵커 이동
- 콘텐츠 0 자격증에서 모의고사 탭 진입 시 크래시 없음 (빈 exams 안전 처리)
