# Anthropic API 키 — enrich/verify 서브커맨드(gen_lecture_audio.py)용.
# 이 파일을 같은 디렉터리에 .env로 복사하고 실제 키를 채우세요:
#   cp .env.example .env
# .env는 .gitignore로 커밋되지 않습니다. 키를 절대 커밋하지 마세요.
# (환경변수 ANTHROPIC_API_KEY가 설정돼 있으면 그쪽이 .env보다 우선합니다.)
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
